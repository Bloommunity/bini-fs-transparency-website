{
	"name": "bloom-transparency-hub",
	"version": "0.1.0",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "node scripts/generate-sheets-data.js && npx nuxi dev",
		"build": "node scripts/generate-sheets-data.js && npx nuxi build",
		"start": "npx nuxi preview",
		"generate-sheets": "node scripts/generate-sheets-data.js"
	},
	"dependencies": {
		"nuxt": "latest",
		"three": "^0.161.0"
	},
	"devDependencies": {
		"tailwindcss": "^3.4.0",
		"autoprefixer": "^10.4.0",
		"postcss": "^8.4.0"
	}
}
